# 测试百度文心一言连接

## 步骤 1：确认配置

当前配置文件 `config/ai-config.js` 已配置为使用百度文心一言：

```javascript
ENABLED_SERVICE: 'baidu',

baidu: {
  name: '百度文心一言',
  apiKey: '您的API Key',
  secretKey: '您的Secret Key',
  model: 'ernie-4.0-turbo-8k',
  ...
}
```

## 步骤 2：重启服务器

```bash
# 停止当前服务器（Ctrl+C），然后运行：
npm start
```

## 步骤 3：通过浏览器测试

### 方法 A：通过 API 接口测试

1. 登录系统（使用 admin 账号）
2. 打开浏览器控制台（F12），执行：

```javascript
// 测试 AI 服务连接
fetch('/api/ai/test-connection', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' }
})
.then(res => res.json())
.then(data => console.log('测试结果:', data));
```

### 方法 B：发送消息测试

1. 登录系统（使用任意账号）
2. 在对话框发送任意消息，查看 AI 回答是否来自百度文心一言

## 步骤 4：预期结果

### 成功时
```json
{
  "success": true,
  "data": {
    "success": true,
    "message": "连接成功！百度文心一言 可用",
    "response": "你好！我是百度文心一言..."
  }
}
```

### 失败时
```json
{
  "success": false,
  "data": {
    "success": false,
    "message": "连接失败: 百度文心一言错误: ..."
  }
}
```

## 常见问题排查

### 问题 1：API Key 格式错误

**错误提示**：获取access_token失败

**原因**：您提供的 API Key 格式可能不正确。百度智能云的 API Key 应该是在"应用列表"中查看的字符串。

**正确获取方式**：
1. 访问 https://console.bce.baidu.com/
2. 进入"AI 智能云" → "应用列表"
3. 创建应用或查看已有应用的 API Key 和 Secret Key

### 问题 2：模型名称错误

**错误提示**：模型不存在

**原因**：当前配置的模型为 `ernie-4.0-turbo-8k`，如果不可用可以尝试其他模型。

**可选模型**：
- `ernie-4.0-turbo-8k`（推荐）
- `ernie-3.5-8k`
- `ernie-speed-8k`

### 问题 3：权限不足

**错误提示**：无权限访问

**原因**：需要在百度智能云控制台开通相应服务。

**解决**：访问 https://console.bce.baidu.com/ai/ 开通文心一言服务

## 手动测试脚本

如果需要手动测试，可以在项目根目录创建 `test.js` 文件：

```javascript
const aiService = require('./services/ai-service');

async function test() {
  const result = await aiService.testConnection();
  console.log(result);
}

test();
```

然后运行：
```bash
node test.js
```
